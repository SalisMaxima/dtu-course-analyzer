# Previous extension build

`dtu-course-analyzer-2.4.0.zip` preserves the committed 2.4.0 extension before
promotion of 2.5.0-beta.3. Unzip it and load its `extension/` directory in Chrome
to restore the previous build. Disable the newer extension first.

The beta builds remain in their existing local `dist/` locations.
